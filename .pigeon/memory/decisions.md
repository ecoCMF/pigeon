# Decision ledger

Every `decisions` entry carried in a handoff, with provenance.
Generated by `pigeon distill`; the handoffs are the source of truth.

## army_macro
- current: "defer to Phase 4 after primitives validated" (session army-design, .pigeon/handoffs/army-design-12.json)

## backward_compat
- current: "all additions opt-in; missing model leaves template unchanged" (session army-design, .pigeon/handoffs/army-design-12.json)

## biggest_risk
- current: "FLAG-1: dynamic task generation in verdict re-entry loop; must be resolved before coordinate.py is touched for re-entry" (session roadmap, .pigeon/handoffs/roadmap-18.json)

## build_order
- current: ["Session 1: Army Phase 1-2", "Session 2: Pillar 3a-3c + Pillar 2a (no deps)", "Session 3: Army Phase 3 (receives:)", "Session 4: Pillar 2b + 2c + 3e", "Session 5: Pillar 2d (FLAG-1 fix agreed) + Pillar 4b-4c", "Session 6: Pillar 3d (if validated) + Pillar 4d"] (session roadmap, .pigeon/handoffs/roadmap-18.json)

## concordance
- current: "express in topology block, not in individual task definitions" (session army-design, .pigeon/handoffs/army-design-12.json)

## cross_wave
- current: "deferred per-task handoff WRITE at spawn (honors append-only); inject upstream pointers into state.artifacts; no supplement files, no schema bump (state.artifacts exists in v1.1)" (session army-design, .pigeon/handoffs/army-design-13.json)

## diff-materialization
- current: "ship raw write to shared-tree coordinate/diffs; drop max_diff_kb until a real failure" (session roadmap, .pigeon/handoffs/roadmap-19.json)

## feedback_loop
- current: "Pillar 3e model scores → Pillar 1 pool ordering; guard with min_runs threshold before applying sort" (session roadmap, .pigeon/handoffs/roadmap-18.json)

## flags
- current: {"FLAG-1": "BLOCK - on-the-fly task generation in re-entry loop violates up-front build invariant and append-only contract; fix: pre-declared reentry:true flag, re-queue same task definition only", "FLAG-2": "BLOCK - verify.auto:true is macro expansion; fix: explicit verification tasks in tasks file only", "FLAG-3": "WATCH - strategy pack layer needs coordinate.reasoning_bank:true gate and 20-session precision validation before default-on", "FLAG-4": "LOW - kill HTML comment provenance injection; replace with .strategies.json sidecar file"} (session roadmap, .pigeon/handoffs/roadmap-18.json)

## hard_deps
- current: "Army Phase 3 (receives:) gates Pillar 2b/2c/2d; Army Phase 2 (pools) gates Pillar 3e; both Pillar 2a and Pillar 3a-3c can start immediately with no deps" (session roadmap, .pigeon/handoffs/roadmap-18.json)

## model-scoring
- current: "offline read-only report; never coordinator-side live pool re-sort (kills starvation feedback loop, preserves determinism)" (session roadmap, .pigeon/handoffs/roadmap-19.json)

## model_field
- current: "model: (concrete) + model_pool: (pool name) as separate optional task fields" (session army-design, .pigeon/handoffs/army-design-12.json)

## model_seam
- current: "separate model: and model_pool: task fields; default runner templates UNCHANGED; preflight rejects unresolved {model}; no -m empty stripping" (session army-design, .pigeon/handoffs/army-design-13.json)

## opencode_lock_fix
- current: "min_spawn_interval_s spaces the army natively; retires the isolated XDG_DATA_HOME workaround" (session pillars-complete, .pigeon/handoffs/pillars-complete-1.json)

## pillars
- current: ["Pillar 1: Multi-Model Army (DECIDED - DESIGN.md)", "Pillar 2: Edit-Review-Fix Pipeline (diff materialization → review schema → regression gate → re-entry)", "Pillar 3: Reasoning Bank (strategy extraction → gate ledger → retrieval → scoring → pack)", "Pillar 4: Observability & Graph Provenance (per-model metrics, strategy/review graph nodes)"] (session roadmap, .pigeon/handoffs/roadmap-18.json)

## pool_roundrobin
- current: "deterministic by task-definition order, no runtime state" (session army-design, .pigeon/handoffs/army-design-12.json)

## preferred_mechanism_cross_wave
- current: "two-phase handoff build (gen-mimo) over supplement handoffs (gen-deepseek) — cleaner, no secondary-file complexity" (session army-design, .pigeon/handoffs/army-design-12.json)

## ranking
- current: "deepseek > mimo > north" (session army-design, .pigeon/handoffs/army-design-12.json)

## rate_limit
- current: "NO in-coordinator TPM/RPM ledger (telemetry is post-exit only); use per-pool max_concurrency + min_spawn_interval_s + budget.deadline_minutes + reactive 429/503 requeue" (session army-design, .pigeon/handoffs/army-design-13.json)

## rate_limit_tracker
- current: "deque sliding-window per model, threading.Lock, config-driven via rate_limited flag" (session army-design, .pigeon/handoffs/army-design-12.json)

## reasoning-bank
- current: "enrich distill markdown + memory scope recall; NO parallel JSON tree, NO 5th pack layer until measured" (session roadmap, .pigeon/handoffs/roadmap-19.json)

## recall-proof
- current: "Phase E keeps outcome-aware pack only if kind:pack saved_tokens net-positive over >=20 sessions" (session roadmap, .pigeon/handoffs/roadmap-19.json)

## review-schema
- current: "reject coordinator validate/merge; optional convention merged by concordance agent only" (session roadmap, .pigeon/handoffs/roadmap-19.json)

## roadmap_status
- current: "complete: Pillars 1-4, phases A-F" (session pillars-complete, .pigeon/handoffs/pillars-complete-1.json)

## throttle_enforced
- current: true (session pillars-complete, .pigeon/handoffs/pillars-complete-1.json)

## topology
- current: "annotation only (display + warn), no macro expansion, no coordinate-layer voting" (session army-design, .pigeon/handoffs/army-design-13.json)
- earlier: "pure annotation only — no DAG rejection; display + auto-injection only" (session army-design, .pigeon/handoffs/army-design-12.json)

## uri_scheme
- current: "repo:// only; model:// scheme from gen-north rejected" (session army-design, .pigeon/handoffs/army-design-12.json)

## verdict-re-entry
- current: "accept gate's fixed pre-declared bounded form but defer to optional last phase F, hard-gated on evidence" (session roadmap, .pigeon/handoffs/roadmap-19.json)

## version
- current: "0.4.0" (session pillars-complete, .pigeon/handoffs/pillars-complete-1.json)

## worktree_paradox
- current: "v1 constraint+preflight warning (cross-wave file artifacts must be on shared tree); git scheme resolver deferred to phase 6" (session army-design, .pigeon/handoffs/army-design-13.json)
